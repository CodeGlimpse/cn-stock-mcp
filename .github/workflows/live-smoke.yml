name: Live Smoke

on:
  workflow_dispatch:
  schedule:
    - cron: "30 0 * * 1-5"

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    concurrency:
      group: live-smoke
      cancel-in-progress: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]

      - name: Check ZHITU_TOKEN
        id: token_check
        env:
          ZHITU_TOKEN: ${{ secrets.ZHITU_TOKEN }}
          EVENT_NAME: ${{ github.event_name }}
        run: |
          if [ -n "$ZHITU_TOKEN" ]; then
            echo "has_token=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_token=false" >> "$GITHUB_OUTPUT"
            if [ "$EVENT_NAME" = "schedule" ]; then
              echo "::error::ZHITU_TOKEN is missing; scheduled live smoke must fail instead of silently skipping."
              exit 1
            fi
            echo "::notice::ZHITU_TOKEN is missing; skipping live smoke tests for manual run."
          fi

      - name: Run live smoke
        if: steps.token_check.outputs.has_token == 'true'
        env:
          ZHITU_TOKEN: ${{ secrets.ZHITU_TOKEN }}
        run: |
          bash scripts/smoke_live.sh
